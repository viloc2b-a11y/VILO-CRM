# Supabase (Project Settings → API) — project ehxciiqxcolnqcohrbrx
NEXT_PUBLIC_SUPABASE_URL=https://ehxciiqxcolnqcohrbrx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (optional; bypasses RLS — never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=

# Vercel Cron + manual POST /api/cron/check-automations (Authorization: Bearer … or x-cron-secret)
CRON_SECRET=
